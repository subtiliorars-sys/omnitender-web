# omnitender-web — Office hours

**Cloud worker:** every 2h, 9–5 weekdays (cron offset :50).  
**Verify:** `python check_web_links.py`  
**Scope:** static public site — no secrets, no pricing/rate claims without owner.

## 5-min PR checklist
1. CI **verify** job green  
2. One wave scope only (`WAVES.md`)  
3. Spot-check changed HTML in browser if copy/funnel wave  
4. Merge same day → next wave auto-picks up  

Sensitive (manual merge): deploy config, payment copy, processor rate claims.
