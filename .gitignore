
# local AWS S3 restore snapshots — never commit
.restore/

# local env files — never commit real credentials
.env
.env.local
.env.*.local

# generated, deploy-time Supabase config (real publishable key injected at deploy)
# never commit the generated artifact — only config.example.js is tracked
config.js
