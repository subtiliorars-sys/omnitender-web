/**
 * Public site config — safe to commit. Fill metaPixelId after Meta Events Manager setup.
 * @see OmniTender/docs/marketing/meta-ads-launch-checklist.md
 */
window.OMNI_SITE_CONFIG = window.OMNI_SITE_CONFIG || {
  /** Paste numeric Pixel ID only (no "px" prefix). Leave empty to disable tracking. */
  metaPixelId: ''
};
